# Souvenote frontend
# No environment variables are required for the current local mock-data build.
#
# Add public client-safe values here when backend/API wiring is introduced, e.g.
# NEXT_PUBLIC_API_BASE_URL=
